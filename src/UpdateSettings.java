/**
 * This interface will allow the viewPanel to update the current settings
 * and will allow the start/pause button to pause and start the game.
 */
public interface UpdateSettings
{
  void updateSettings();
}
